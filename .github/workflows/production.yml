name: Production deploy

on:
  schedule:
    # # 10:05, 10:35から22:05, 22:35まで、05分と35分に実行
    - cron: '5,35 1-14 * * *'
  workflow_dispatch:

env: 
  target_environment: ${{ vars.TARGET_ENVIRONMENT }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build project
      run: |
        echo "Hello $target_environment"
        echo "Hello $TARGET_ENVIRONMENT"
        echo "Hello ${{ vars.ENVIRONMENT_STAGE }}"
        echo "Hello ${{ env.target_environment }}"
        echo "Hello ${{ env.TARGET_ENVIRONMENT }}"

